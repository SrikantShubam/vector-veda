{
  "name": "Vector Veda",
  "private": true,
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "tinacms dev -c \"node scripts/next-dev-no-fork.cjs\"",
    "dev:next": "node scripts/next-dev-no-fork.cjs",
    "build": "next build --no-lint",
    "build:next": "next build --no-lint",
    "start": "npm run build:next && next start",
    "start:next": "next start",
    "tina:dev": "tinacms dev -c \"next dev\"",
    "tina:build": "tinacms build && next build"
  },
  "dependencies": {
    "@tinacms/cli": "^1.12.6",
    "gray-matter": "^4.0.3",
    "marked": "^9.1.6",
    "next": "^15.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-icons": "^5.5.0",
    "tinacms": "^2.10.1"
  }
}
