/* Local stub for Framer analytics script.
   Phase 2 keeps runtime fully local by replacing remote tracking load. */
(function () {
  if (typeof window === "undefined") return;
  window.__framerEventsStub = true;
})();
