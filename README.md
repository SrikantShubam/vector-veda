# Vector Veda

## Commands
- `npm install`
- `npm run dev`
- `npm run build:next`
- `npm run start`

## Runtime
- Use Node.js `20.x` (see `.nvmrc`).
- `npm run start` requires a successful build first (`npm run build:next`).
